<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.aac">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".idVO"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aac"
        tools:ignore="Instantiatable">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".word1Activity" />
        <activity android:name=".word2Activity" />
        <activity android:name=".loginActivity" />
        <activity android:name=".addidActivity" />
        <activity android:name=".editWordActivity" />
        <activity android:name=".fractCUD" />
        <activity android:name=".fractAddActivity" />
        <activity android:name=".fractDelActivity" />
        <activity android:name=".fractUpdateActivity" />
        <activity android:name=".fractUpdateSelectActivity" />
        <activity android:name=".wordCUD" />
        <activity android:name=".wordAddActivity" />
        <activity android:name=".wordDelActivity" />
        <activity android:name=".wordUpdateActivity" />
        <activity android:name=".wordUpdateSelectActivity" />
        <activity android:name=".wordPicActivity" />
        <activity android:name=".wordPicSelectActivity" />
    </application>

</manifest>